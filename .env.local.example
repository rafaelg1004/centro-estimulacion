# Archivo de configuración para desarrollo local
# Copia este archivo como .env en la carpeta centro-estimulacion/

# URL del backend local
REACT_APP_API_URL=http://localhost:4000/api

# Entorno de desarrollo
NODE_ENV=development

# Puerto del frontend (opcional)
PORT=3000

# Ejemplo para producción:
# NODE_ENV=production
# (En producción no se necesita REACT_APP_API_URL porque usa la URL por defecto)
